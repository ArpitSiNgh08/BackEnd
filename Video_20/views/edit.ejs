<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md text-sm mb-5 inline-block px-3 py-2" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="Whats on your mind" class="w-1/3 p-3 outline-none block resize-none border-2 border-zinc-600 rounded-md" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-500 text-black block rounded-md mt-2 w-40" type="submit" value="Update post">
        </form>
        
    </div>
</body>
</html>